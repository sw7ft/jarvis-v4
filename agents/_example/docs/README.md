# Agent docs

This directory is for **deep reference material** the agent writes as it works:

- Site maps and infrastructure notes
- Client procedures and contacts
- Research summaries
- Module deploy records

The agent (or you) create files here over time. Nothing is required at deploy.

Example: after analyzing a website, the agent might add `docs/site-map.md`.
